<% title t(:tags) %>

<table>
  <tr>
    <th><%= t :field_label %></th>
    <th></th>
    <th></th>
  </tr>

<% @tags.each do |tag| %>
  <tr>
    <td><%= tag.label %></td>
    <td><%= link_to t(:link_edit), edit_tag_path(tag) %></td>
    <td><%= link_to t(:link_destroy), tag, :confirm => t(:confirm_sure), :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to t(:tag_new), new_tag_path %>
